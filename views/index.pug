extends layout

block content
  h1= title
  p Welcome to the Page of Sravanthi!
  p I am a student at NWMSU!
  button(type="button", class="btn btn-secondary",onclick = "doReduce()") Press Me
  br
  a(href='http://localhost:3000/dogs') Click here to visit dogs website!
  div#reduce Reduce the value
   script(type='text/javascript').
    let count = 20
    let change = -1
    function doReduce(){
      count += change;
      change -= 1;
      document.getElementById("reduce").innerHTML='Count '+count + "with change "+ change
    }
